# Gradle Properties
  org.gradle.jvmargs = -Xmx1G
  org.gradle.parallel = true

# Mod Properties
  version = 1_01
  maven_group = ca.rttv
  archives_base_name = ecosystemic

# Dependencies are managed at gradle/libs.versions.toml
